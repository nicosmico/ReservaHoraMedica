import { RutToNamePipe } from './rut-to-name.pipe';

describe('RutToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RutToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
