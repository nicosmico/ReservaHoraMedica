import { ExamenIdToNamePipe } from './examen-id-to-name.pipe';

describe('ExamenIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ExamenIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
